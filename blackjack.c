//BaekJoon #2798 by yoonseul at 210714

#include <stdio.h>

int main(void) {
	int card, standard;
	int num[100];
	int MAX;

	scanf("%d %d", &card, &standard);

	for (int i = 0; i < card; i++) {
		scanf("%d", num[i]);
	}

	for (int i = 0; i < card; i++) {
		for (int j = 0; j < card - 1; j++) {
			for	(int k = 0; k < card - 2; k++) {

			}
		}
	}
}